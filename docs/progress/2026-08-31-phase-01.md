# Phase 01 progress — domain model, units, signs, and statistics

## Objective and inputs

Implement only pure domain behavior from approved Phase 00 commit `c93cc95b54079058e7aab2961374d693a19b1ebe`. Inputs read before implementation: `AGENTS.md`, `AGENTS.vi.md`, mandatory reading-order documents, Phase 00 plan/progress, requirements, evidence baseline, source map/ledger, traceability, risk register, and status.

## Source and evidence discipline

No new external claim was introduced. Existing Phase 00 provenance and immutable evidence remain authoritative. `tests/fixtures/ktamv-t3-evidence.json` is labelled evidence-only and records its Phase 00 manifest; it is not a domain default. No raw `evidence/imported` file changed.

## Files changed

- `src/xyz_klipper_tool/domain/`: typed units/vectors/frames/signs, provider-separated models, verdicts/reason codes, statistics, state machine, schema codec.
- `schemas/switch-measurement-result.v1.schema.json`.
- `tests/`: unit, table-driven, metamorphic, fault, schema, and T3 evidence-fixture tests.
- Paired Phase 01 plan, status, test matrix, and traceability documents.
- `artifacts/test-runs/phase-01-domain/results.json` and `manifest.json`.

## Exact commands and results

- `$env:PYTHONPATH='src'; python -m unittest discover -s tests -v` — PASS, 11 tests.
- `python scripts/check_markdown_pairs.py` — PASS.
- `python scripts/check_requirements_traceability.py` — PASS, 24 unique IDs.
- `python scripts/check_source_ledger.py` — PASS, 11 bilingual pinned rows.
- `python scripts/check_license_artifact.py` — PASS.
- `python scripts/check_master_prompt_parity.py` — PASS, 18 sections/42 headings.
- Schema JSON parse — PASS.
- Static domain import scan — PASS; no framework/filesystem/network/service imports.
- Secret scan — PASS.
- `git diff --check c93cc95b54079058e7aab2961374d693a19b1ebe HEAD` — PASS.
- Governance negative-fixture suites — PASS; expected failures were detected in temporary fixtures. Direct checker output for those fixtures is intentionally FAIL, while the test suites exit PASS.

## Decisions and risks

The estimator excludes `INVALID` samples, preserves ordered raw observations, and makes outlier filtering explicit and opt-in. n=0/n=1 return typed insufficient-sample results; sample SD is `null` rather than raising. Apply/freshness/rollback are immutable data only; no writer or machine side effect exists. Physical provider sign compatibility, station safety, motion envelopes, and operator abort remain `REQUIRES_HIL`.

## Status and gate

Status: `IMPLEMENTED` offline, candidate only; supervisor decision is `PENDING`. Implementation commit: `6313de1b1fcbb74d6247bb9c171f7d6e7e26b67f`. Branch: `phase/01-domain`, to be pushed to `origin/phase/01-domain`. Remediation/closure record is the next commit after artifact generation. Next gate: supervisor review; do not merge `main` or claim physical safety.

# Tiến độ Phase 01 — mô hình miền, đơn vị, dấu và thống kê

## Mục tiêu và đầu vào

Chỉ triển khai hành vi miền thuần túy từ commit Phase 00 đã duyệt `c93cc95b54079058e7aab2961374d693a19b1ebe`. Đã đọc trước implementation: `AGENTS.md`, `AGENTS.vi.md`, tài liệu theo thứ tự bắt buộc, plan/progress Phase 00, requirements, evidence baseline, source map/ledger, traceability, risk register và status.

## Kỷ luật nguồn và bằng chứng

Không đưa claim bên ngoài mới. Provenance Phase 00 và evidence bất biến hiện có vẫn là nguồn chi phối. `tests/fixtures/ktamv-t3-evidence.json` được gắn nhãn chỉ-bằng-chứng và ghi manifest Phase 00; không phải default miền. Không file raw nào dưới `evidence/imported` bị thay đổi.

## File đã thay đổi

- `src/xyz_klipper_tool/domain/`: unit/vector/frame/sign có kiểu, model riêng provider, verdict/mã lý do, statistics, state machine, schema codec.
- `schemas/switch-measurement-result.v1.schema.json`.
- `tests/`: test unit, table-driven, metamorphic, fault, schema và fixture evidence T3.
- Plan Phase 01, status, test matrix và traceability song ngữ.
- `artifacts/test-runs/phase-01-domain/results.json` và `manifest.json`.

## Lệnh chính xác và kết quả

- `$env:PYTHONPATH='src'; python -m unittest discover -s tests -v` — PASS, 11 test.
- `python scripts/check_markdown_pairs.py` — PASS.
- `python scripts/check_requirements_traceability.py` — PASS, 24 ID duy nhất.
- `python scripts/check_source_ledger.py` — PASS, 11 row pin song ngữ.
- `python scripts/check_license_artifact.py` — PASS.
- `python scripts/check_master_prompt_parity.py` — PASS, 18 mục/42 heading.
- Parse JSON schema — PASS.
- Scan import miền tĩnh — PASS; không import framework/filesystem/network/service.
- Scan secret — PASS.
- `git diff --check c93cc95b54079058e7aab2961374d693a19b1ebe HEAD` — PASS.
- `ruff check src tests` — PASS; `ruff format --check src tests` — PASS.
- Bộ fixture âm governance — PASS; đã phát hiện failure kỳ vọng trong fixture tạm. Output checker trực tiếp cho fixture đó cố ý là FAIL, còn test suite kết thúc PASS.

## Quyết định và rủi ro

Estimator loại sample `INVALID`, giữ observation raw theo thứ tự, và chỉ filter outlier khi policy khai báo/opt-in. n=0/n=1 trả kết quả thiếu mẫu có kiểu; sample SD là `null`, không raise. Apply/freshness/rollback chỉ là data bất biến; chưa có writer hay side effect máy. Tương thích dấu provider vật lý, an toàn station, envelope di chuyển và abort operator vẫn là `REQUIRES_HIL`.

## Trạng thái và cổng

Trạng thái: `PASS`; quyết định supervisor là `PASS` cho commit đã review `9d58fecb6cc19342c1bcd9dd62eafb8bf03c1a0d`. Nhánh: `phase/01-domain`. Cổng kế tiếp: lập kế hoạch Phase 02; không merge `main` và không tuyên bố an toàn vật lý.

## Closure hành chính

Gate chính xác đã được supervisor duyệt: 16/16 test pin, coverage 95%, mypy, pyright, Ruff, schema, governance, hash, link, secret, bất biến raw evidence và kiểm tra remote branch đều PASS. Hành vi vật lý vẫn `REQUIRES_HIL`. Closure này chỉ thay metadata quyết định trong tài liệu và test artifact.

## Correction pass của supervisor

Đã thêm Seconds/Celsius và validation finite; vector mang frame/sign cùng sign conversion fail-closed; context XY chỉ dành cho camera; observation đủ hierarchy và series key có kiểu; result/codec/schema Z Switch và Cartographer Touch tách biệt; chặn trộn provider/axis/tool/identity; raw bất biến cùng summary unfiltered/filtered, rejection có lý do, insufficiency tường minh, limit đã validate, reference drift có kiểu và contract test. Fixture evidence T3 hiện ánh xạ ba giá trị vào `cycle-1` đến `cycle-3` ở hierarchy `OUTER_CYCLE`, không đổi value hay provenance. Đã thêm runtime validation enum cho hierarchy và sample status, đồng thời để `summarize` tạo/so sánh `StatisticSeriesKey` nhưng cho phép aggregate các outer cycle. Đã thêm `requirements-dev.txt` pin cùng cấu hình mypy/pyright. Kết quả correction chính xác: `python -m coverage run -m unittest discover -s tests` — PASS, 16 test; `python -m coverage report -m` — PASS, TOTAL 95%; `ruff check src tests` — PASS; `ruff format --check src tests` — PASS; test AST docstring public API — PASS. Validate Draft 2020-12 bằng `jsonschema==4.23.0` đã pin — PASS cho positive và fault missing/type/enum/provider/axis/non-finite. Trong venv cô lập `C:\Users\batca\AppData\Local\Temp\xyz-klipper-tool-phase01-venv-20260831`, `python -m pip install -r requirements-dev.txt` — PASS; `python -m mypy src/xyz_klipper_tool tests` — `Success: no issues found in 11 source files`; `python -m pyright src/xyz_klipper_tool tests` — `0 errors, 0 warnings, 0 informations`. Type gate đã đạt; vẫn chờ supervisor re-review.

# Phase 02 progress — ports, fake adapters, simulator, and station persistence

## Objective and inputs

Implement only Phase 02 from supervisor-approved checkpoint `checkpoint-phase-01`, closure commit `9417f69efbd57be0ca35241e65745f19c16ed15a`, on branch `phase/02-ports-simulator`. Mandatory English/Vietnamese governance, Phase 00/01 plans and progress, requirements, traceability, evidence baseline, source map/ledger, risk register, and status were read before implementation.

## Files and architecture

- `src/xyz_klipper_tool/ports/`: typed hexagonal contracts.
- `src/xyz_klipper_tool/adapters/`: deterministic no-I/O fakes for printer, tools, camera, detector, Z, stores, clock, lock, and offset writer sentinel.
- `src/xyz_klipper_tool/stations/`: provider-separated station models and teach/show/clear use cases.
- `src/xyz_klipper_tool/persistence/`: versioned JSON envelope, checksum, atomic temp/flush/fsync/replace, bounded backup, and fail-closed reader.
- `src/xyz_klipper_tool/configuration.py` and `tool_selection.py`: canonical redacted fingerprints and dynamic bounded tool selection.
- `schemas/station-envelope.v1.schema.json`, `tests/test_phase02.py`, paired plan and governance updates.

## Decisions and boundaries

SAFE_Z omission fails closed with `UNSAFE_APPROACH`; current Z is never inferred as clearance. Station coordinates require explicit `CurrentPose` input. Camera, Switch, and Cartographer namespaces are separate. No real hardware, Klipper/Moonraker/OpenCV, production config, motion, heating, probing, tool change, restart, apply, or HIL occurred.

## Exact validation and results

- Pinned venv `$env:PYTHONPATH='src'; C:\Users\batca\AppData\Local\Temp\xyz-klipper-tool-phase01-venv-20260831\Scripts\python.exe -m coverage run -m unittest discover -s tests` — PASS, 24 tests.
- `$env:PYTHONPATH='src'; C:\Users\batca\AppData\Local\Temp\xyz-klipper-tool-phase01-venv-20260831\Scripts\python.exe -m coverage report -m` — PASS, TOTAL 90%.
- `$env:PYTHONPATH='src'; C:\Users\batca\AppData\Local\Temp\xyz-klipper-tool-phase01-venv-20260831\Scripts\python.exe -m mypy src/xyz_klipper_tool tests` — PASS, `Success: no issues found in 24 source files`.
- `$env:PYTHONPATH='src'; C:\Users\batca\AppData\Local\Temp\xyz-klipper-tool-phase01-venv-20260831\Scripts\python.exe -m pyright src/xyz_klipper_tool tests` — PASS, `0 errors, 0 warnings, 0 informations`.
- `python -m ruff check src tests` — PASS; `python -m ruff format --check src tests` — PASS, 24 files already formatted.
- `python scripts/check_markdown_pairs.py` — PASS.
- `python scripts/check_requirements_traceability.py` — PASS, 29 unique IDs.
- `python scripts/check_source_ledger.py` — PASS, 11 bilingual pinned rows.
- `python scripts/check_license_artifact.py` — PASS.
- `python scripts/check_master_prompt_parity.py` — PASS, 18 sections/42 headings.
- `git diff --check` — PASS; raw `evidence/imported` unchanged; artifact JSON/schema validation PASS.

## Status, risks, and next gate

Status: `IMPLEMENTED` offline candidate; supervisor decision `PENDING`. Risks remaining: filesystem/platform atomicity, printer/tool state compatibility, travel safety, provider semantics, and physical compatibility remain `REQUIRES_HIL` or later integration work. Exact test artifact and SHA-256 manifest are under `artifacts/test-runs/phase-02-ports-simulator/`. Next gate is supervisor review; no checkpoint tag before PASS.

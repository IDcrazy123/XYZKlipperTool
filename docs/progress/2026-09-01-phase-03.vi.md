# Tiến độ Phase 03 — capture camera, calibration và framework detector

## Phạm vi và audit evidence

Bắt đầu từ commit `origin/main` `f0283a3c756d728c6b66a2775d02ed24d4d97cd5` trên `phase/03-camera-cv`; working tree sạch trước implementation. Audit chỉ đọc đã kiểm tra `D:\Desktop\All-Config-Voron-main\Voron 5 Tool\extras\experiments` (chỉ CSV/Markdown), `D:\Desktop\All-Config-Voron-main\Voron 5 Tool\extras\backups\pre-toolvision-ux-hil-20260826-161315\Generated-Data-ToolVision\tool-vision-history` (chỉ JSON lịch sử) và `evidence/imported` (CSV/Markdown XY và JSON Z). Không tìm thấy frame camera thật đã sanitize; không đọc hoặc sao chép source legacy.

## Implementation

Đã thêm capture camera qua transport được inject với retry/timeout hữu hạn, giới hạn byte/kích thước/pixel và kết quả typed; decode PGM theo geometry; hai pipeline candidate connected-component và circle-like; model/store calibration checksum; context identity/freshness; diagnostic hữu hạn xác định; benchmark gắn nhãn `SYNTHETIC`, inventory corpus bất biến và split theo session. Các trường Transform là image-to-mm tương đối, không là origin máy/station. Không có I/O camera/network/printer hay HIL. SRC-009/SRC-010/SRC-013/SRC-014 là tham chiếu OpenCV chính thức; heuristic/threshold first-party là assumption thiết kế chưa validate và không sao chép code upstream.

## Validation và trạng thái

Trạng thái tài liệu correction 5E: chạy 47 test, 47 PASS, skip=0; coverage source-only là 90%. Fixture P5 đã commit có bốn session; seed 17 với holdout ratio 0.5 và development ratio 0.25 cho calibration=session-d, development=session-b và HOLDOUT=session-c cùng session-a (một negative, một positive có nhãn). Kết quả thực tế của BlobDetector và CircleCandidateDetector trên HOLDOUT đều TP=0, FN=1, TN=1, precision=0.0, recall=0.0, với `ZERO_CANDIDATE`; kết quả synthetic này không phải evidence reliability. Đánh giá corpus thật đã sanitize còn thiếu và có trạng thái `NEEDS_WORK`; độ bền thư mục platform vẫn `OPEN`; tương thích camera vật lý riêng biệt là `REQUIRES_HIL`. Cổng kế tiếp là supervisor review; không bắt đầu Phase 04.

Lệnh validation exact: `venv pinned; $env:PYTHONPATH='src'; python -m unittest discover -s tests` -> `Ran 47 tests ... OK`; `venv pinned; $env:PYTHONPATH='src'; python -m coverage erase; python -m coverage run -m unittest discover -s tests; python -m coverage report -m --include='src/*'` -> `TOTAL 1729 171 90%`; `venv pinned; PYTHONPATH=src; python -m mypy src/xyz_klipper_tool tests` -> `Success: no issues found in 30 source files`; `venv pinned; PYTHONPATH=src; python -m pyright src/xyz_klipper_tool tests` -> `0 errors, 0 warnings, 0 informations`.
## Đóng phục hồi HIL offline

Ngày 2026-09-05, việc đối chiếu offline giữ nguyên dữ liệu HIL một phần hiện có, không đổi byte raw. Frame T0 nguồn 8086 vẫn là `INVALID/WRONG_CAMERA_SOURCE` và bị loại. Ba frame T1 VoronBed vẫn là `OBSERVED_LED_ON` và bị loại khỏi corpus LED-off và corpus thuật toán. Không capture hoặc tuyên bố frame T2 nào. Lượt tiếp tục T2 dừng fail-closed sau truy vấn LED-off thật vì `toolchanger.tool_number=-1` mâu thuẫn với `detected_tool_number=2`; kiểm tra supervisor sau đó cũng thấy `homed_axes` rỗng tại X170 Y20 Z40 đứng yên. Lượt phục hồi này không ra lệnh cho máy.

Kiểm tra phục hồi exact: `Get-FileHash -Algorithm SHA256` cùng đối chiếu metadata trên cả bốn JPEG -> 4/4 khớp byte-size và SHA-256; số lượng -> raw_jpegs=4, invalid=1, observed_led_on=3, accepted_led_off=0, t2_frames=0. Lượt tiếp tục sau đó cũng dừng tại usage-limit gate. Bản ghi máy đọc được là `evidence/hil/phase-03/partial-run-manifest.json` và `user-provided-picture-inventory.json`; báo cáo dừng song ngữ là `evidence/hil/phase-03/ABORT_REPORT.md` và `.vi.md`. Phase 03 vẫn `NEEDS_WORK`; tương thích vật lý và việc tiếp tục vẫn `REQUIRES_HIL`. Điều kiện kế tiếp là operator cho phép rõ ràng mới và home thành công `xyz`.
## Correction adapter JPEG thật

Đã thêm adapter OpenCV JPEG chỉ phía host tại `src/xyz_klipper_tool/vision/jpeg_adapter.py` và kiểm tra candidate pixel archive riêng. Đường sau dùng source root/manifest/output/ROI explicit, kế thừa warning metadata, ghi overlay full-frame/development-ROI và không bao giờ chấp nhận nozzle hay đo calibration. `scripts/explore_picture.py` đã retire và không còn tạo calibration hay đọc input. 17 ảnh user không được test đọc, chỉnh sửa, di chuyển hoặc commit.

Gate correction exact: venv pin `python -m unittest discover -s tests` -> `Ran 54 tests ... OK`; `python -m mypy --python-version 3.12 src/xyz_klipper_tool tests scripts/explore_picture.py` -> `Success: no issues found in 33 source files`; `python -m pyright src/xyz_klipper_tool tests scripts/explore_picture.py` -> `0 errors, 0 warnings, 0 informations`; coverage source-only -> `TOTAL 1943 191 90%`; Ruff check/format trên adapter, test và explorer đã sửa -> PASS; lệnh schema Draft 2020-12 positive/negative -> PASS. Lệnh exploratory read-only đã chạy trên bốn path JPEG explicit của user; output gắn `USER_PROVIDED_UNLABELED`, `metric_claim=false`, không ghi file.

Correction bounded cuối vẫn là `NEEDS_WORK`: CLI archive hiện từ chối image rỗng/quá cỡ/hỏng và ROI ngoài giới hạn trước khi diagnostic, xuất record pixel-only với `uncertainty_mm=null` và `shape_score=null` khi thiếu calibration, và không claim detector đủ điều kiện. Report full-frame và development-ROI explicit chỉ là diagnostic; không raw capture hay file `picture/` của user nào bị sửa đổi.

Gate closure: `C:\Temp\xyz-klipper-tool-phase03-venv-20260905\Scripts\python.exe -m unittest discover -s tests` -> `Ran 60 tests ... OK`; coverage source-only -> `TOTAL 2003 202 90%`; mypy -> `Success: no issues found in 35 source files`; pyright -> `0 errors, 0 warnings, 0 informations`; Ruff/governance/parity/license/hash/diff -> PASS. Tooling OpenCV host là `IMPLEMENTED`; frame thật vẫn chỉ development vì thiếu calibration/ground truth, và Phase 03 vẫn `NEEDS_WORK`.

Correction input contract archive: CLI nhận dạng versioned canary `frames[]` và HIL `items[]` được giữ lại, resolve metadata trong root được cung cấp, giữ `capture_status`, `invalid_reason`, `corpus_inclusion`, `verdict`, thời gian UTC và trạng thái HTTP nguồn, đồng thời fail-closed khi metadata thiếu/không hỗ trợ/hỏng. Replay HIL được giữ là 1 `INVALID/WRONG_CAMERA_SOURCE` và 3 `OBSERVED_LED_ON`, tất cả bị loại.

## Ingress phát triển cho capture thật đã tổ chức

Sau đó trong ngày 2026-09-05, lượt acquisition được operator cho phép rõ ràng đã tạo hai session VoronBed có tổ chức trong thư viện ngoài `D:\Desktop\XYZKlipperTool-Captures`: 15 frame baseline L001 và 60 frame stress-sweep L004/L016/L064/L255, ba frame cho mỗi tổ hợp tool/mức sáng T0–T4 đã quan sát. JPEG capture được tách trong `01_PHOTOS`; manifest và metadata capture bất biến ở `80_EVIDENCE`; phân tích dẫn xuất ở `90_TESTS_ANALYSIS`. T0–T4 cùng station/mức sáng quan sát là bằng chứng session, không phải default sản phẩm hard-code. Lượt tiếp tục implementation offline này không gửi lệnh camera, network, máy in, đổi tool, đèn, restart hay deployment.

Archive inspector giờ chỉ nhận đúng một trong các shape `frames[]`, `items[]` hoặc thư viện capture `photos[]` có version. Ingestion `photos[]` yêu cầu manifest chưa accepted/bị loại, path/tên/text canonical có bound, timestamp UTC-Z, SHA-256 chữ thường, byte count, kích thước, content type JPEG và khoảng độ sáng vòng; kích thước decode và byte được đối chiếu, còn traversal, collection mơ hồ hoặc record đã accepted đều dừng fail-closed. Output giữ provenance session/tool/light/frame nhưng ép `accepted_nozzle=false`, `machine_eligibility=false` và `source_corpus_inclusion=EXCLUDED`.

Replay độc lập đã kiểm tra lại byte count và hash của 75/75 JPEG (21.617.906 byte, không lỗi), đồng thời tạo 75 overlay full-frame và 75 overlay development-ROI explicit trong `90_TESTS_ANALYSIS/20260905_Phase03_PixelDiagnostics_75frames_v1`. Cả 75 record vẫn là `WARNING`. Trong ROI `[620,230,220,240]`, số contour chung từ 3 đến 28 trên mỗi frame; L255 có tỷ lệ saturation grayscale trung bình cao nhất (`0.18979040404040404`). Đây là quan sát, không phải nhãn nozzle hay chấp nhận tuning. Kết quả đã sanitize là `artifacts/test-runs/phase-03-camera-cv/real-capture-development-analysis.json`; report chứa path và JPEG raw vẫn ở ngoài Git.

Cổng corpus được siết để `evaluate_benchmark` từ chối entry `UNLABELED`, mẫu positive thiếu tâm đã review và mẫu negative lại có tâm. Tọa độ tâm pixel phải hữu hạn, không âm trong cả contract Python và JSON Schema. Hai session thật vẫn thiếu ground truth tâm nozzle đã review, identity/uncertainty calibration và session thứ ba độc lập đã gán nhãn để chia calibration/development/holdout, nên Phase 03 vẫn `NEEDS_WORK`.

Gate ingress hiện tại: venv đã pin `python -m unittest discover -s tests` -> `Ran 71 tests ... OK`; coverage chỉ source -> `TOTAL 2010 203 90%`; mypy -> `Success: no issues found in 35 source files`; pyright -> `0 errors, 0 warnings, 0 informations`; Ruff check/format, parity Markdown, traceability 36 ID, source ledger 15 dòng, artifact GPL, parity master prompt, kiểm tra hash 23 file evidence bất biến, scan credential trên file thay đổi, parse JSON và `git diff --check` -> PASS.

# Tiến độ Phase 03 — capture camera, calibration và framework detector

## Phạm vi và audit evidence

Bắt đầu từ commit `origin/main` `f0283a3c756d728c6b66a2775d02ed24d4d97cd5` trên `phase/03-camera-cv`; working tree sạch trước implementation. Audit chỉ đọc đã kiểm tra `D:\Desktop\All-Config-Voron-main\Voron 5 Tool\extras\experiments` (chỉ CSV/Markdown), `D:\Desktop\All-Config-Voron-main\Voron 5 Tool\extras\backups\pre-toolvision-ux-hil-20260826-161315\Generated-Data-ToolVision\tool-vision-history` (chỉ JSON lịch sử) và `evidence/imported` (CSV/Markdown XY và JSON Z). Không tìm thấy frame camera thật đã sanitize; không đọc hoặc sao chép source legacy.

## Implementation

Đã thêm capture camera qua transport được inject với retry/timeout hữu hạn, giới hạn byte/kích thước/pixel và kết quả typed; decode PGM theo geometry; hai pipeline candidate connected-component và circle-like; model/store calibration checksum; context identity/freshness; diagnostic hữu hạn xác định; benchmark gắn nhãn `SYNTHETIC`, inventory corpus bất biến và split theo session. Các trường Transform là image-to-mm tương đối, không là origin máy/station. Không có I/O camera/network/printer hay HIL. OpenCV SRC-009/SRC-010 vẫn là evidence ứng viên; không sao chép code upstream.

## Validation và trạng thái

Candidate correction 5: chạy 43 test, 43 PASS, skip=0; coverage source-only là 90% (`TOTAL 1706 statements, 178 missed, 90%`). Đã thêm envelope/schema calibration hữu hạn, checksum, hook fault xoay backup, inventory corpus hash/path tương đối, split toàn session và metrics inject cho cả hai pipeline ứng viên. Đây chỉ là cơ chế offline; chưa có corpus thật đã sanitize. Gate holdout/reliability corpus thật vẫn `NEEDS_WORK`; độ bền thư mục platform vẫn `OPEN`; tương thích camera vật lý vẫn `REQUIRES_HIL`. Cổng kế tiếp là supervisor review; không bắt đầu Phase 04.

Lệnh validation exact: `venv pinned; $env:PYTHONPATH='src'; python -m unittest discover -s tests` -> `Ran 43 tests ... OK`; `venv pinned; $env:PYTHONPATH='src'; python -m coverage erase; python -m coverage run -m unittest discover -s tests; python -m coverage report -m --include='src/*'` -> `TOTAL 1706 178 90%`; `venv pinned; PYTHONPATH=src; python -m mypy src/xyz_klipper_tool tests` -> `Success: no issues found in 30 source files`; `venv pinned; PYTHONPATH=src; python -m pyright src/xyz_klipper_tool tests` -> `0 errors, 0 warnings, 0 informations`.

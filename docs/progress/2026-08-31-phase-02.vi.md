# Tiến độ Phase 02 — port, fake adapter, simulator và persistence station

## Mục tiêu và đầu vào

Chỉ triển khai Phase 02 từ checkpoint `checkpoint-phase-01` đã duyệt, commit closure `9417f69efbd57be0ca35241e65745f19c16ed15a`, trên nhánh `phase/02-ports-simulator`. Đã đọc trước implementation các tài liệu governance Anh/Việt bắt buộc, plan/progress Phase 00/01, requirements, traceability, evidence baseline, source map/ledger, risk register và status.

## File và kiến trúc

- `src/xyz_klipper_tool/ports/`: contract hexagonal có kiểu.
- `src/xyz_klipper_tool/adapters/`: fake xác định, không I/O cho printer, tool, camera, detector, Z, store, clock, lock và offset-writer sentinel.
- `src/xyz_klipper_tool/stations/`: model station tách provider và use case teach/show/clear.
- `src/xyz_klipper_tool/persistence/`: envelope JSON có version, checksum, temp/flush/fsync/replace atomic, backup hữu hạn và reader fail-closed.
- `src/xyz_klipper_tool/configuration.py` và `tool_selection.py`: fingerprint canonical che secret và chọn tool động bounded.
- `schemas/station-envelope.v1.schema.json`, `tests/test_phase02.py`, plan song ngữ và cập nhật governance.

## Quyết định và biên

Bỏ SAFE_Z thì fail closed bằng `UNSAFE_APPROACH`; không suy ra Z hiện tại là clearance. Tọa độ station bắt buộc đến từ input `CurrentPose` tường minh. Namespace Camera, Switch và Cartographer tách biệt. Không chạy hardware thật, Klipper/Moonraker/OpenCV, config production, motion, heating, probing, đổi tool, restart, apply hay HIL.

## Xác minh chính xác và kết quả

- Venv pin `$env:PYTHONPATH='src'; C:\Users\batca\AppData\Local\Temp\xyz-klipper-tool-phase01-venv-20260831\Scripts\python.exe -m coverage run -m unittest discover -s tests` — PASS, 27 test.
- `$env:PYTHONPATH='src'; C:\Users\batca\AppData\Local\Temp\xyz-klipper-tool-phase01-venv-20260831\Scripts\python.exe -m coverage report -m` — PASS, tổng 91%.
- `$env:PYTHONPATH='src'; C:\Users\batca\AppData\Local\Temp\xyz-klipper-tool-phase01-venv-20260831\Scripts\python.exe -m mypy src/xyz_klipper_tool tests` — PASS, `Success: no issues found in 24 source files`.
- `$env:PYTHONPATH='src'; C:\Users\batca\AppData\Local\Temp\xyz-klipper-tool-phase01-venv-20260831\Scripts\python.exe -m pyright src/xyz_klipper_tool tests` — PASS, `0 errors, 0 warnings, 0 informations`.
- `python -m ruff check src tests` — PASS; `python -m ruff format --check src tests` — PASS, 24 file đã format.
- `python scripts/check_markdown_pairs.py` — PASS.
- `python scripts/check_requirements_traceability.py` — PASS, 29 ID duy nhất.
- `python scripts/check_source_ledger.py` — PASS, 11 row pin song ngữ.
- `python scripts/check_license_artifact.py` — PASS.
- `python scripts/check_master_prompt_parity.py` — PASS, 18 mục/42 heading.
- `python -c "import json, jsonschema; ..."` — PASS, xác thực schema Draft 2020-12 với FormatChecker; kiểm tra import-boundary domain, secret assignment, raw-evidence diff và SHA-256 cũng PASS bằng các lệnh riêng được ghi trong `results.json`.

## Trạng thái, rủi ro và cổng kế tiếp

Trạng thái: candidate offline `IMPLEMENTED`; quyết định supervisor `PENDING` cho commit correction `ce99ca14b4df5e870a79364ef188884c0394dc65`. Rủi ro OPEN rõ ràng: durability thư mục theo nền tảng là best-effort và chưa triển khai như một bảo đảm phổ quát; tương thích printer/tool vật lý vẫn `REQUIRES_HIL`. Không đưa ra tuyên bố an toàn vật lý. Artifact test và manifest SHA-256 nằm trong `artifacts/test-runs/phase-02-ports-simulator/`. Không tạo tag, merge, release hay hành động máy thật.

# Phase 03 progress — camera capture, calibration, and detector framework

## Scope and evidence audit

Started from `origin/main` commit `f0283a3c756d728c6b66a2775d02ed24d4d97cd5` on `phase/03-camera-cv`; working tree was clean before implementation. Read-only audit checked `D:\Desktop\All-Config-Voron-main\Voron 5 Tool\extras\experiments` (CSV/Markdown only), `D:\Desktop\All-Config-Voron-main\Voron 5 Tool\extras\backups\pre-toolvision-ux-hil-20260826-161315\Generated-Data-ToolVision\tool-vision-history` (historical JSON only), and repository `evidence/imported` (XY CSV/Markdown and Z JSON only). No sanitized real camera frames were found; no legacy source was read or copied.

## Implementation

Added bounded injected-transport camera capture with finite retries/timeout, byte/dimension/pixel limits and typed outcomes; PGM decode geometry; connected-component and circle-like candidate pipelines; calibration model/checksummed store; identity/freshness context; deterministic bounded diagnostics; benchmark output labeled `SYNTHETIC`; immutable corpus inventory; and session-separated split tooling. Transform fields are relative image-to-mm values and never machine/station origins. No camera/network/printer I/O or HIL occurred. SRC-009/SRC-010/SRC-013/SRC-014 are official OpenCV references only; first-party heuristics and thresholds are unvalidated design assumptions and no upstream code was copied.

## Validation and status

Correction 5E documentation state: 47 tests executed, 47 PASS, skipped=0; source-only coverage is 90%. The committed P5 fixture has four sessions; seed 17 with holdout ratio 0.5 and development ratio 0.25 yields calibration=session-d, development=session-b, and HOLDOUT=session-c plus session-a (one negative and one labeled positive). Actual BlobDetector and CircleCandidateDetector results on HOLDOUT are both TP=0, FN=1, TN=1, precision=0.0, recall=0.0, with `ZERO_CANDIDATE`; this synthetic result is not reliability evidence. The missing sanitized real-corpus evaluation is `NEEDS_WORK`; platform directory durability remains `OPEN`; physical camera compatibility is separately `REQUIRES_HIL`. Next gate is supervisor review; do not begin Phase 04.

Exact validation commands: `pinned venv; $env:PYTHONPATH='src'; python -m unittest discover -s tests` -> `Ran 47 tests ... OK`; `pinned venv; $env:PYTHONPATH='src'; python -m coverage erase; python -m coverage run -m unittest discover -s tests; python -m coverage report -m --include='src/*'` -> `TOTAL 1729 171 90%`; `pinned venv; PYTHONPATH=src; python -m mypy src/xyz_klipper_tool tests` -> `Success: no issues found in 30 source files`; `pinned venv; PYTHONPATH=src; python -m pyright src/xyz_klipper_tool tests` -> `0 errors, 0 warnings, 0 informations`.
## Offline HIL recovery closure

On 2026-09-05, offline reconciliation preserved the existing partial HIL data without changing raw bytes. The 8086-source T0 frame remains `INVALID/WRONG_CAMERA_SOURCE` and excluded. The three VoronBed T1 frames remain `OBSERVED_LED_ON` and excluded from the LED-off and algorithm corpora. No T2 frame was captured or claimed. The attempted T2 continuation stopped fail-closed after the true LED-off query because `toolchanger.tool_number=-1` conflicted with `detected_tool_number=2`; a later supervisor check also observed empty `homed_axes` at stationary X170 Y20 Z40. The machine was not commanded by this recovery turn.

Exact recovery checks: `Get-FileHash -Algorithm SHA256` plus metadata reconciliation over all four JPEGs -> 4/4 byte-size and SHA-256 matches; counts -> raw_jpegs=4, invalid=1, observed_led_on=3, accepted_led_off=0, t2_frames=0. The later continuation also stopped at the usage-limit gate. Machine-readable records: `evidence/hil/phase-03/partial-run-manifest.json` and `user-provided-picture-inventory.json`; paired abort report: `evidence/hil/phase-03/ABORT_REPORT.md` and `.vi.md`. Phase 03 remains `NEEDS_WORK`; physical compatibility and continuation remain `REQUIRES_HIL`. Next prerequisite is fresh explicit operator authorization and successful `xyz` homing.
## Real JPEG adapter correction

Added the host-only OpenCV JPEG adapter at `src/xyz_klipper_tool/vision/jpeg_adapter.py` and separate archived pixel candidate inspection. The latter uses explicit source roots/manifests/output/ROI, inherits metadata warnings, writes full-frame/development-ROI overlays, and never accepts a nozzle or calibration measurement. `scripts/explore_picture.py` is retired and no longer constructs calibration or reads input. The 17 user-provided images were not read by tests, edited, moved, or committed.

Exact correction gates: pinned venv `python -m unittest discover -s tests` -> `Ran 54 tests ... OK`; `python -m mypy --python-version 3.12 src/xyz_klipper_tool tests scripts/explore_picture.py` -> `Success: no issues found in 33 source files`; `python -m pyright src/xyz_klipper_tool tests scripts/explore_picture.py` -> `0 errors, 0 warnings, 0 informations`; source-only coverage -> `TOTAL 1943 191 90%`; Ruff check/format on modified adapter, tests, and explorer -> PASS; Draft 2020-12 positive/negative schema command -> PASS. Exploratory read-only command ran on four explicit user JPEG paths; outputs were labeled `USER_PROVIDED_UNLABELED` with `metric_claim=false`, and no files were written.

Final bounded correction remains `NEEDS_WORK`: the archive CLI now rejects empty/oversized/corrupt images and out-of-bounds ROIs before diagnostics, emits pixel-only records with `uncertainty_mm=null` and `shape_score=null` when calibration is unavailable, and does not claim detector eligibility. Full-frame and explicit development-ROI reports are diagnostic-only; no raw capture or user `picture/` file is modified.

Closure gate: `C:\Temp\xyz-klipper-tool-phase03-venv-20260905\Scripts\python.exe -m unittest discover -s tests` -> `Ran 60 tests ... OK`; source-only coverage -> `TOTAL 2003 202 90%`; mypy -> `Success: no issues found in 35 source files`; pyright -> `0 errors, 0 warnings, 0 informations`; Ruff/governance/parity/license/hash/diff -> PASS. OpenCV host tooling is `IMPLEMENTED`; real frames remain development-only without calibration/ground truth, and Phase 03 remains `NEEDS_WORK`.

Archive input-contract correction: the CLI accepts the versioned canary `frames[]` and preserved HIL `items[]` shapes, resolves metadata under the supplied root, preserves source `capture_status`, `invalid_reason`, `corpus_inclusion`, `verdict`, UTC capture time, and HTTP evidence state, and fails closed on missing/unsupported/malformed metadata. Preserved HIL replay remains 1 `INVALID/WRONG_CAMERA_SOURCE` plus 3 `OBSERVED_LED_ON`, all excluded.

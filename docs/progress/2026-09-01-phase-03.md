# Phase 03 progress — camera capture, calibration, and detector framework

## Scope and evidence audit

Started from `origin/main` commit `f0283a3c756d728c6b66a2775d02ed24d4d97cd5` on `phase/03-camera-cv`; working tree was clean before implementation. Read-only audit checked `D:\Desktop\All-Config-Voron-main\Voron 5 Tool\extras\experiments` (CSV/Markdown only), `D:\Desktop\All-Config-Voron-main\Voron 5 Tool\extras\backups\pre-toolvision-ux-hil-20260826-161315\Generated-Data-ToolVision\tool-vision-history` (historical JSON only), and repository `evidence/imported` (XY CSV/Markdown and Z JSON only). No sanitized real camera frames were found; no legacy source was read or copied.

## Implementation

Added bounded injected-transport camera capture with finite retries/timeout, byte/dimension/pixel limits and typed outcomes; PGM decode geometry; connected-component and circle-like candidate pipelines; calibration model/checksummed store; identity/freshness context; deterministic bounded diagnostics; benchmark output labeled `SYNTHETIC`; immutable corpus inventory; and session-separated split tooling. Transform fields are relative image-to-mm values and never machine/station origins. No camera/network/printer I/O or HIL occurred. OpenCV SRC-009/SRC-010 remain candidate evidence; no upstream code was copied.

## Validation and status

Current candidate tests: 37 tests executed, 35 PASS and 2 skipped; source-only coverage (`python -m coverage report -m --include='src/*'`) is 90%. Pinned mypy 1.14.1 and pyright 1.1.390 pass on source and tests; Ruff, schema and governance checks pass. Capture and decode timeout semantics are separate, frame identity is bound to metadata, and detection invariants reject non-finite/unbounded values. Real-corpus holdout/reliability gate is `NEEDS_WORK`; physical camera compatibility remains `REQUIRES_HIL`. Directory/source raw evidence was not modified. Next gate is supervisor review; do not begin Phase 04.

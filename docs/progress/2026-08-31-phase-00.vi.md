# Tiến độ Phase 00 — 2026-08-31

- Mục tiêu: governance, audit nguồn, license, nhập evidence và nền parity.
- Input: bootstrap; CSV/report kTAMV; 20 JSON trong tool-vision-history; 1 JSON invalid ở backup pre-resume.
- Nguồn: SOURCE_LEDGER.md; HEAD upstream pin ngày 2026-08-31.
- Evidence: 2 file XY và 21 file Z nhập nguyên byte; đã tạo manifest.
- Quyết định: slug `xyz_klipper_tool`; GPL-3.0-or-later; không mã đo production.
- Validation: Markdown parity PASS; manifest 23/23 PASS; so raw PASS; secret scan PASS; parse/status JSON 21/21 PASS; diff check PASS; không vi phạm boundary production. Đã audit link local của Markdown do kho viết.
- Trạng thái: `IMPLEMENTED` cho deliverable offline Phase 00; tương thích vật lý vẫn `REQUIRES_HIL`.
- Commit/nhánh remote: `64c9b6daea5c2577980fbfe93253607986962caa`, `632aaf5` / `origin/phase/00-governance`.
- Cổng kế tiếp: supervisor review; không merge `main`.

## Sửa parity theo supervisor — 2026-08-31

- Phạm vi: chỉ dịch đầy đủ `PROJECT_BUILD_PROMPT.vi.md`.
- Lệnh validation: `python scripts/check_master_prompt_parity.py`
- Kết quả: `MASTER PROMPT PARITY: PASS (18 sections, 42 headings, all code/command tokens present)`.
- Kiểm tra thêm: `python scripts/check_markdown_pairs.py` → `Markdown pair check: PASS`; `git diff --check` → PASS.

## Sửa checker theo supervisor — 2026-08-31

- Phạm vi: chỉ checker cặp Markdown hai chiều và fixture âm tạm thời.
- Validation: `python scripts/test_markdown_pairs.py` → `Markdown pair negative fixtures: PASS (missing VI and missing EN both fail)`.
- Validation: `python scripts/check_markdown_pairs.py` → `Markdown pair check: PASS`.
- Validation: `python scripts/check_markdown_pairs.py --root <temporary fixture root>` có output ổn định `path: reason` và exit nonzero khi thiếu một phía; root tạm tự xóa.
- Validation: `git diff --check` → PASS; không có fixture trong kho.

## Sửa requirements/traceability theo supervisor — 2026-08-31

- Phạm vi: chỉ độ bao phủ requirements, traceability và checker/test ID của chúng.
- Validation: `python scripts/check_requirements_traceability.py` → `Requirements traceability check: PASS (24 unique IDs, exact bilingual and matrix match)`.
- Validation: `python scripts/test_requirements_traceability.py` → `Requirements traceability negative fixtures: PASS (missing/extra and duplicate IDs detected)`.
- Validation: `python scripts/check_markdown_pairs.py` → `Markdown pair check: PASS`; `git diff --check -- . ':(exclude)LICENSE'` → PASS. License chính thức giữ blank line cuối bắt buộc, nên whitespace checker của Git báo warning.

## Sửa license artifact theo supervisor — 2026-08-31

- Phạm vi: chỉ `LICENSE` chuẩn, wording SPDX/decision, tính nhất quán notices và checker license artifact.
- Nguồn: https://www.gnu.org/licenses/gpl-3.0.txt, truy cập 2026-08-31; SHA-256 source tải về `3972dc9744f6499f0f9b2dbf76696f2ae7ad8af9b23dde66d6af86c9dfb36986`; SHA-256 artifact LF chuẩn hóa trong kho `e57f1c320b8cf8798a7d2ff83a6f9e06a33a03585f6e065fea97f1d86db84052`.
- Validation: `python scripts/check_license_artifact.py` → `License artifact check: PASS (official GPL-3.0 text, normalized SHA-256, warranty and ending verified)`.
- Validation: `python scripts/check_markdown_pairs.py` → `Markdown pair check: PASS`; `git diff --check -- . ':(exclude)LICENSE'` → PASS. License chính thức giữ blank line cuối bắt buộc, nên whitespace checker của Git báo warning.
- Fixture âm nằm trong thư mục temp hệ thống và tự xóa; không còn trong kho.

## Sửa source ledger theo supervisor — 2026-08-31

- Phạm vi: chỉ source ledger, third-party notices, record pin/license provenance và kiểm tra của chúng.
- Xác minh nguồn sơ cấp: Klipper `f0892d82`, Moonraker `985c1d0`, Cartographer docs `b0519c0`, OpenCV 4.12.0 deref `49486f6`, kTAMV `72421f2`, KTC `b880e37`, klipper-toolchanger `94756df`.
- Validation: `python scripts/check_source_ledger.py` → `Source ledger check: PASS (11 bilingual pinned source rows)`.
- Validation: `python scripts/test_source_ledger.py` → `Source ledger negative fixtures: PASS (placeholder fields fail in temp root)`.
- Validation: `python scripts/check_markdown_pairs.py` → `Markdown pair check: PASS`; `git diff --check` → PASS.

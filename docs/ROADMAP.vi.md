# Lộ trình

Phase 00 governance/evidence → 01 domain thuần → 02 ports/fake/station → 03 camera/CV → 04 cycle X/Y → 05 Z switch → 06 Z Cartographer → 07 apply/rollback có duyệt → 08 host API → 09 lệnh Klippy → 10 installer → 11 docs release/security → 12 HIL có giám sát.

Phase 03 là candidate implementation offline hiện tại; đang chờ supervisor review. Gate holdout corpus thật là `NEEDS_WORK`; HIL, an toàn vật lý và production readiness vẫn `REQUIRES_HIL`/`NOT READY`.
